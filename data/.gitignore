# Runtime/user state — not committed.
uploads/
topics.json

# data/library/*.json, data/hadith/*.md, and data/quran.md are the real
# Quran/Hadith corpus fetched by scripts/fetch-library.mjs (CC0-style, from
# fawazahmed0/quran-api and fawazahmed0/hadith-api). They ARE committed so
# the app and Vercel deploys are self-contained — regenerate any time with:
#   node scripts/fetch-library.mjs
